Text-based RPG written entirely in Java, asks for simple keyboard input and prints relevant output in console.

TODO: add .cmd files to build all classes and provide simple way to run in user's console.